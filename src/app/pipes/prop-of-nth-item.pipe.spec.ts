import { PropOfNthItemPipe } from './prop-of-nth-item.pipe';

describe('PropOfNthItemPipe', () => {
  it('create an instance', () => {
    const pipe = new PropOfNthItemPipe();
    expect(pipe).toBeTruthy();
  });
});
