import { ConvertSecsPipe } from './convert-secs.pipe';

describe('ConvertSecsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSecsPipe();
    expect(pipe).toBeTruthy();
  });
});
