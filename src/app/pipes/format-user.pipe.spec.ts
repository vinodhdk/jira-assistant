import { FormatUserPipe } from './format-user.pipe';

describe('FormatUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatUserPipe();
    expect(pipe).toBeTruthy();
  });
});
